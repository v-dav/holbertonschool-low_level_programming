#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: an integer to multiply
 * @b: an integer to multiply
 *
 * Return: an integer result of multiplication of other integers
 */
int mul(int a, int b)
{
	return (a * b);
}
