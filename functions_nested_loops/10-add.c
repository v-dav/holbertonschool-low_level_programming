#include "main.h"

/**
 *add - a function that adds two integers and returns the result.
 *@a: - an integer
 *@b: - an integer
 *
 * Return: result of addition of 2 integers
 */
int add(int a, int b)
{
	int resultat = a + b;

	return (resultat);
}
