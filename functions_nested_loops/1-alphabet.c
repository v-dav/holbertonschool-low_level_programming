#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Description: prints the alphabet, in lowercase, followed by a new line.
 * Return: 0 success
 */
int main(void)
{
	print_alphabet();
	return (0);
}
